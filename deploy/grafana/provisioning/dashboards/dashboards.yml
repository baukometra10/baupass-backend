apiVersion: 1
providers:
  - name: BauPass
    orgId: 1
    folder: BauPass
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    options:
      path: /etc/grafana/provisioning/dashboards/json
