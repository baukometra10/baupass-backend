apiVersion: 1
providers:
  - name: WorkPass
    orgId: 1
    folder: WorkPass
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    options:
      path: /etc/grafana/provisioning/dashboards/json
